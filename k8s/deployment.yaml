apiVersion: apps/v1
kind: Deployment
metadata:
  name: human-benchmark
  namespace: human-benchmark
  labels:
    app: human-benchmark
spec:
  replicas: 3
  selector:
    matchLabels:
      app: human-benchmark
  template:
    metadata:
      labels:
        app: human-benchmark
    spec:
      containers:
      - name: human-benchmark
        image: msalekmouad/humanbenchmark:amd64
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/conf.d/default.conf
          subPath: nginx.conf
      volumes:
      - name: nginx-config
        configMap:
          name: nginx-config
