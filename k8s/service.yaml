apiVersion: v1
kind: Service
metadata:
  name: human-benchmark-service
  namespace: human-benchmark
  labels:
    app: human-benchmark
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: human-benchmark
