apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: human-benchmark

resources:
- namespace.yaml
- configmap.yaml
- deployment.yaml
- service.yaml
- ingress.yaml
- hpa.yaml

# Alternative ingress without security headers (use if snippets are disabled)
# - ingress-simple.yaml

commonLabels:
  app: human-benchmark
  version: v1.0.0
